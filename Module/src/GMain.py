#================================================
import math
#================================================
m_sqrt = math.sqrt(5)
m_pow = math.pow(5, 2)
m_format = ""

m_format += "m_sqrt : " + str(m_sqrt) + "\n"
m_format += "m_pow : " + str(m_pow) + "\n"

print(m_format)
#================================================
